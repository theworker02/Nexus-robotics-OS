# Skill 2.0

Skills declare required and optional capabilities, safety class, timeout, recovery behavior, supported runtime profiles, and verification level (`UNTESTED`, `SIMULATED`, `HIL_TESTED`, `HARDWARE_VALIDATED`). Simulation success must never be presented as physical-hardware validation.
