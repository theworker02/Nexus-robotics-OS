# Warehouse fetch

`nexus sim scenario warehouse-fetch --no-ai` starts the NXR-2 deterministic simulator, finds the blue container, performs a simulated grasp, delivers it to Station B, and records replay events. The route is software-tested and simulation-tested only.
