# Contributing

Use stable Rust, keep new adapters behind Nexus core interfaces, validate untrusted manifests, and add a test for behavior changes. Run `cargo test --workspace` before opening a pull request. Never add secrets, robot credentials, or unsafe direct actuator paths to the repository.
