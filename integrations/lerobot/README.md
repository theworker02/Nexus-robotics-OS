# LeRobot compatibility adapter

The adapter retains episode timestamps, action vectors, observation state vectors, camera references, and metadata. It is a data-contract bridge—not a replacement for LeRobot training or policy tooling.
