'use client';
import { useState } from 'react';
export default function SenseHoppingPage() { const [depthOnline, setDepthOnline] = useState(true); const active = depthOnline ? 'RGB-D + LiDAR fusion' : 'LiDAR fallback'; return <main><nav className="nav"><a className="brand" href="/"><span className="mark">N</span> NEXUS</a><div className="links"><a href="/">Platform</a><a href="/structurescan">StructureScan</a><a href="/active-learning">Active Learning</a></div></nav><section className="hero"><p className="eyebrow">SENSEHOPPING</p><h1>Perception that<br/>adapts.</h1><p className="lede">Request information, not a fixed sensor. Nexus selects healthy providers, fuses compatible modalities, and records every switch.</p></section><section className="feature-grid"><div className="sim-card"><p className="eyebrow">INTERACTIVE SIMULATION</p><h2>Door geometry</h2><div className="signal"><span className={depthOnline ? 'live' : 'offline'} /> RGB-D {depthOnline ? 'ACTIVE' : 'UNAVAILABLE'}</div><div className="signal"><span className="live" /> LiDAR STANDBY</div><button onClick={() => setDepthOnline(!depthOnline)}>{depthOnline ? 'Disable depth camera' : 'Restore depth camera'}</button></div><div className="feature-copy"><p className="panel-label">CURRENT SENSE PLAN</p><h2>{active}</h2><p>{depthOnline ? 'Depth and lidar are fused for redundant spatial geometry.' : 'Depth is unavailable. LiDAR supplies the distance and geometry estimate so the task can continue.'}</p><ul><li>Health-aware routing</li><li>Confidence and latency ranking</li><li>Replayable fallback evidence</li></ul></div></section><footer><span className="brand"><span className="mark">N</span> NEXUS</span><p>Simulation-first sensing, not sensor certification.</p></footer></main>; }
